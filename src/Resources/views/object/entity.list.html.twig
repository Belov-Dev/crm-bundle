{% extends '@A2CRM/layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-2">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                    <tr>
                        <th>Name</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td><a href="{{ path('a2crm_object_list', {'objectName': entity.name}) }}">{{ entity.name }}</a></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}